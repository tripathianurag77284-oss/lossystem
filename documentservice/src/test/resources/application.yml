spring:
  datasource:
    url: jdbc:h2:mem:documentdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

document:
  storage:
    directory: target/test-docs
