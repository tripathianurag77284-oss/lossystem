spring:
  application:
    name: customerservice

  datasource:
    url: jdbc:postgresql://localhost:5432/lossystem
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true



server:
  port: 8084